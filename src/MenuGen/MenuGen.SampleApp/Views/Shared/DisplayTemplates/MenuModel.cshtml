@model MenuGen.Core.MenuModel

<ul>
    @foreach (var node in Model.MenuNodes)
    {
        <li>
            @Html.DisplayFor(m => node)
            @if (node.ChildMenu != null && node.ChildMenu.MenuNodes != null && node.ChildMenu.MenuNodes.Any())
            {
                @Html.DisplayFor(x => node.ChildMenu);
            }
        </li>
    }
</ul>